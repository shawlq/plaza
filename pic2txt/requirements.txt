# 默认按 GPU 安装（与 process_by_ocr.py 默认走 GPU 一致）。
#
# PyTorch：从官方 CUDA 12.4 轮子源拉取（与 NVIDIA 驱动兼容即可，具体见 pytorch.org）。
# Paddle：从飞桨官方 cu118 源拉取 paddlepaddle-gpu（与脚本中 PaddleOCR 2.x 搭配）。
# 若你本机 CUDA/驱动与下述 index 不匹配，请改 cu124 / cu118 或查阅各自安装文档后替换 URL。
#
# 从 CPU 版改到 GPU 版后，建议在干净环境中重装；已有环境里可先卸载再装：
#   pip uninstall -y torch torchvision torchaudio paddlepaddle paddlepaddle-gpu
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu118/

torch>=2.0.0,<3
torchvision>=0.15.0
transformers>=4.36.0
accelerate>=0.25.0
Pillow>=9.0.0


# ocr_image_to_text.py（RapidOCR / ONNXRuntime，CPU 友好）
onnxruntime>=1.15.0
rapidocr-onnxruntime>=1.3.0
